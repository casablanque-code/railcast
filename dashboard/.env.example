# Dashboard and API are now served by the same Worker, so requests are
# relative by default (leave this unset/empty in production). Only set this
# for local `next dev` when testing against a deployed Worker on a
# different origin.
NEXT_PUBLIC_API_BASE=
